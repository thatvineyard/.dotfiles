
(setq package-user-dir "~/.dotfiles/.emacs.d/elpa")
(package-initialize)

;; Emacs Load Path
(setq default-directory "~/.dotfiles/.emacs.d")
(add-to-list 'load-path "~/.dotfiles/.emacs.d")
(load-library "init")
